# Copy to .env before `docker compose up`.

# Remote inference via Fireworks. Leave empty to run local-only.
FIREWORKS_API_KEY=

# Comma-separated Ollama models pulled at first boot.
# Swap for the models revealed at the hackathon kickoff.
OLLAMA_MODELS=gemma3:1b
