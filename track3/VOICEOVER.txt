Universities already pay fortunes for plagiarism detection. But plagiarism tools can only tell you if text was copied — never whether it's true. This essay looks perfectly fine. Three of its claims are fabricated. And one of its best sentences was stolen.

(pause)

This is a content-integrity suite running natively inside Drupal — the CMS these universities already run. One click runs three checks. A factcheck extracts every factual claim and verifies it against the university's own indexed corpus: the founding date — contradicted. The lost manuscripts — contradicted. The Nobel Prize that never happened — contradicted. True claims come back supported, with the evidence shown. An AI-writing likelihood score estimates who wrote it. And a verbatim plagiarism search catches the stolen sentence — here, a famous line from Darwin, found word for word across the web, with the source URL.

(pause)

Every inference call — claim extraction and verdict checking — runs on an AMD Instinct GPU: ROCm and vLLM serving Google's Gemma as an OpenAI-compatible endpoint. Here's the decision log: every call, every model, every routing decision, audited. Student work never has to leave university-controlled AMD hardware.

(pause)

And everything is configuration, not code. Which models. Which evidence sources. Which domains your institution trusts — with reputations backed by citable external assessments — and which domains count against a claim when they echo it. The software has no opinion; your institution does, and the audit trail shows it. It's GPL, published on drupal dot org as the Universal AI Provider module. Here is the full source and the factcheck implementation on the project page. For any university already on Drupal, this is a module install away.

(pause)

Drupal AI Factchecker. Claim-level truth, where the content lives, on AMD.
